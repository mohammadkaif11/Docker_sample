---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "green-service"
  namespace: "default"
  labels:
    app: "green"
spec:
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 8080
  selector:
    app: "green"
  type: "LoadBalancer"
  loadBalancerIP: ""
